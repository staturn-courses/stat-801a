\item Answer the following questions on-line by checking either \textbf{True or False.}
\begin{enumerate}
\item The t-distribution is characterized by the parameters $\mu$ (the mean) and $\sigma$ (the standard deviation). \red{False}
\item The t-table (Table D) works with the area above (right or upper tail) while the z-table (Table A) works with the area below (left or lower tail). \red{True}
\item The t-distribution approaches the Standard Normal Distribution, $N(0,1)$, as the degrees of freedom increase because $s$ estimates $\sigma$ with less precision when the sample size increases. \red{False}
\end{enumerate}

