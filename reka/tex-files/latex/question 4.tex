\item \textbf{True or False.}
\begin{enumerate}
\item The t-distribution is characterized by the parameters $\mu$ (the mean) and $\sigma$ (the standard deviation). 
%\newline \textbf{Answer: F}
\item The t-table works with the area above (right or upper tail) while the z-table (standard normal table) works with the area below (left or lower tail).
%\newline \textbf{Answer: T}
\item The t-distribution approaches the Standard Normal Distribution, $N(0,1)$, as the degrees of freedom increase because $s$ estimates $\sigma$ with less precision when the sample size increases. 
%\newline \textbf{Answer: F}
\end{enumerate}

